# Decisions

## Self-hosted autopilot line #decision #autopilot #selfhost

We chose to keep the autopilot line self-hosted and local-first so the portable
constitution stays auditable and depends on no hosted service. Rationale: trust is
the product; the hosted layer is optional, fail-closed, and never load-bearing.

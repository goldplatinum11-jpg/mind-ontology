# Direction

## Ship the unified checkout #direction #storefront #checkout

The current priority is a unified checkout across the storefront and billing
service, so the team ships one flow instead of two divergent ones.

## Harden the data platform #direction #data #reliability

The platform should move from ad-hoc jobs to scheduled, observable pipelines with
clear ownership per transform step.
